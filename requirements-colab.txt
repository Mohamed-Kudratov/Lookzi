# Google Colab requirements.
#
# Deliberately does NOT pin torch: Colab ships a working CUDA build already, and
# reinstalling torch==2.7.0+cu128 costs ~3 GB and forces a runtime restart for
# no benefit. Everything else follows environment.yml.
#
# Install with:
#   pip install -r requirements-colab.txt
#   pip install -e ./diffusers
#   pip install easy-dwpose==1.0.2 --no-deps      # has conflicting pins of its own

accelerate==1.10.1
bitsandbytes>=0.46.0
einops
gradio
huggingface-hub==0.34.4
hf-xet==1.1.9
omegaconf==2.3.0
onnxruntime==1.22.0
opencv-python==4.11.0.86
peft==0.17.1
pillow==11.0.0
protobuf==6.32.0
safetensors==0.6.2
sentencepiece==0.2.1
timm==1.0.16
tokenizers==0.22.0
transformers==4.56.0
tqdm==4.67.1

# easy-dwpose pulls these in but we install it with --no-deps
matplotlib==3.10.6
scipy
