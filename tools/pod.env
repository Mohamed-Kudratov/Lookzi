# The pod this session is working against. Sourced by hand, not committed to
# any expectation that it stays true -- a pod is a rental, and the address
# changes every time one is opened.
export POD_HOST=c3z3js7v6rns85-64412093@ssh.runpod.io
